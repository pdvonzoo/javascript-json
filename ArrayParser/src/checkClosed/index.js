const isArrayClosed = require('../checkClosed/isArrayClosed/isArrayClosed')
const isObjClosed = require('../checkClosed/isObjClosed/isObjClosed')

module.exports = Object.freeze({
    isArrayClosed,
    isObjClosed,
  });